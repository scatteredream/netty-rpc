server:
  port: 8881
rpc:
  client:
    load-balance: random
    serialization: protostuff
    transport: netty
    registry: zookeeper
    registry-addr: 39.108.66.202:2181
    timeout: 5000
    max-retries: 4
    base-delay: 1000

# 设置控制台日志显示级别
logging:
  level:
    com.wxy.rpc: info